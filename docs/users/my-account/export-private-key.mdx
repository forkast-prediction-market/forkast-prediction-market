---
title: Export Private Key
description: Backup or migrate your wallet with care
icon: Key
---

import { Steps, Step } from 'fumadocs-ui/components/steps'

{process.env.NEXT_PUBLIC_DISCORD_LINK && (
<Callout>
  Need Help? For assistance, reach out to us on <a href={process.env.NEXT_PUBLIC_DISCORD_LINK} target="_blank">Discord</a>.
</Callout>
)}

<Callout type="warning">
<strong>Important Security Warning:</strong> Never share your private key with anyone. {process.env.NEXT_PUBLIC_SITE_NAME} will <strong>never</strong> ask for your private key.
</Callout>

## Export instructions

<Steps>
  <Step>
    <h3 className="!mt-0">Secure your environment</h3>
    <p>
      Use a trusted device on a private network before exporting sensitive
      wallet data.
    </p>
  </Step>
  <Step>
    <h3 className="!mt-0">Verify your identity</h3>
    <p>
      Confirm your password and 2FA (if enabled) to authorize the export.
    </p>
  </Step>
  <Step>
    <h3 className="!mt-0">Save securely</h3>
    <p>
      Store the private key in an encrypted vault or password manager. Never
      share it via email, chat, or screenshots.
    </p>
  </Step>
</Steps>

<Callout type="info">
Export is intended for wallet backup or migration. If you only need to change login methods, you usually don't need to export your key.
</Callout>
